# Copy this file to .env and fill in real values. .env is gitignored.

# Google Maps SDK key (Android + iOS), used by react-native-maps.
# Create one at https://console.cloud.google.com/google/maps-apis/credentials
# for the same GCP project as Firebase (artiverse-440712), with the
# "Maps SDK for Android" and "Maps SDK for iOS" APIs enabled.
GOOGLE_MAPS_API_KEY=
